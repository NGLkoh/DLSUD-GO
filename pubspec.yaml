name: dlsud_go
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.1.1
  mapbox_maps_flutter: ^2.12.0
  geolocator: ^14.0.2
  http: ^0.13.6
  flutter_dotenv: ^5.1.0
  flutter_tts: ^4.0.2
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.3
  dialog_flowtter: ^0.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

  assets:
    - .env
    - assets/dialogflow_key.json
